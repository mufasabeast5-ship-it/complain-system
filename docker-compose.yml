version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: careconnect_backend
    ports:
      - "3000:80"
    volumes:
      - .:/var/www/html
    env_file:
      - .env
